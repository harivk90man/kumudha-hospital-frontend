/**
 * Public surface of the front-desk app shell. The root mounts the routes
 * via `routes.tsx`; internal pages are not re-exported because they’re
 * rendered exclusively through the route tree.
 */
export { frontdeskRoutes } from './frontdeskRoutes';
